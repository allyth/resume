import React, { useState, useRef } from 'react';
import Resume from './Resume';

export const App = () => {
    return (
        <div className="App">
            <Resume></Resume>
        </div>
    );
}

export default App;
